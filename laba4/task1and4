public class task1and4  // Завдання 1 і 4 пов’язані з обробкою масивів чисел, деректива яка підключае бібліотеку
{
    public static void main(String[] args) // масив рядків тобто набір текстових значень та ім'я зміної яка містить цей масив.args використовується для передачі даних до програми через командний рядок.
    // [] - Це параметр методу main, який дозволяє програмі отримувати аргументи з командного рядка під час запуску.
    //Масив — це послідовність змінних одного типу, зібраних під однією назвою
    {
        int[] evenArray = new int[50]; // створення масивів на 50 елементів для того щоб не вводити їх вручну (проміжний масив) якій юуде заповнений автоматично черех цикл
        int[] oddArray = new int[50];

        int even = 2; // починається з 2 (перше парне число)
        int odd = 1; //починається з 1 (перше непарне число)

        // Заповнюємо масиви
        for (int i = 0; i < 50; i++) //цикл який пройде 50 разів (від i = 0 до i = 49 включно).
        {
            evenArray[i] = even; //Кожен елемент масиву отримує поточне значення
            even += 2; // Потім збільшується на 2 (наступне парне число)

            oddArray[i] = odd; // те саме але для парних чисел
            odd += 2;
        }

        // Вивід початкових масивів
        System.out.println("Початковий масив парних чисел:"); // введеня значень на єеран і перехід до наступного рядка (бібліотека-змінна-метод)
        printArray(evenArray); // Викликаємо метод printArray, щоб надрукувати всі елементи evenArray

        System.out.println("\nПочатковий масив непарних чисел:");
        printArray(oddArray); //Виводимо текст: "Початковий масив непарних чисел:" і друкуємо oddArray

        // Зміна знаку в непарних числах (тобто в oddArray)
        for (int i = 0; i < oddArray.length; i++) // Другий цикл for проходить по всьому oddArray
        {
            oddArray[i] = -oddArray[i]; //Кожен елемент змінюється на протилежний (- перед числом змінює знак).
        }

        System.out.println("\nМасив непарних чисел після зміни знаку:");
        printArray(oddArray); //Після зміни виводимо новий масив непарних чисел.
    }

    // Метод для виводу масиву
    public static void printArray(int[] array) //Оголошення окремого методу printArray, який друкує елементи масиву.
    {
        for (int num : array) //Для кожного числа num в масиві array вивести його разом з пробілом
        {
            System.out.print(num + " ");
        }
        System.out.println();
    }
}
