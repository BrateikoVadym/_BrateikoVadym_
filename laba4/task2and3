public class task2and3 // Завдання 2 і 3 повязане з перебором і підрахунками , деректива яка підключае бібліотеку
{
    public static void main(String[] args) // масив рядків тобто набір текстових значень та ім'я зміної яка містить цей масив.args використовується для передачі даних до програми через командний рядок.
    // [] - Це параметр методу main, який дозволяє програмі отримувати аргументи з командного рядка під час запуску.
    //Масив — це послідовність змінних одного типу, зібраних під однією назвою
    {
        int[] array = {2, 17, 13, 6, 22, 31, 45, 66, 100, -18}; //створення масиву з 10 конкретних чисел.

        // Цикл while
        System.out.println("Перебір циклом while:");
        int i = 0;
        while (i < array.length)  //length - довжина масиву
        {
            System.out.print(array[i] + " ");
            i++;
        } //Поки i менше довжини масиву Виводимо елемент array[i]. Збільшуємо i на 1.

        // Цикл for
        System.out.println("\n\nПеребір циклом for:");
        for (int j = 0; j < array.length; j++) {
            System.out.print(array[j] + " "); //Перебираємо масив циклом for, друкуємо всі елементи.
        }

        // Цикл while з непарними індексами
        System.out.println("\n\nНепарні індекси (while):");
        i = 0; //Переустановлюємо i = 0
        while (i < array.length) //Виводимо тільки ті елементи, індекс яких непарний (i % 2 != 0)
        {
            if (i % 2 != 0) //! - зміню на протележне
            {
                System.out.print(array[i] + " ");
            }
            i++;
        }

        // Цикл for з парними індексами
        System.out.println("\n\nПарні індекси (for):"); //Перебір циклом for, виводимо тільки парні індекси (j % 2 == 0)
        for (int j = 0; j < array.length; j++) {
            if (j % 2 == 0) {
                System.out.print(array[j] + " ");
            }
        }

        // Зворотній порядок Цикл for, який іде від останнього елемента до першого.
        System.out.println("\n\nМасив у зворотньому порядку:");
        for (int j = array.length - 1; j >= 0; j--) //Наприклад, якщо масив містить 5 елементів, то індекси елементів будуть 0, 1, 2, 3, 4, а останній елемент знаходиться за індексом 4 (тобто array.length - 1 = 5 - 1 = 4)
        {
            System.out.print(array[j] + " ");
        }

        // Сума елементів
        int sum = 0; //Обчислюємо суму всіх елементів масиву Додаємо кожен елемент (value) до sum.Виводимо суму.


        for (int value : array) {
            sum += value;
        }
        System.out.println("\n\nСума всіх елементів масиву: " + sum);
    }
}
